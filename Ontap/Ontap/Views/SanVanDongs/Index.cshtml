@model IEnumerable<Ontap.Data.SanVanDong>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenSan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenSan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaSan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaSan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaSan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
